# TESTER FILE - BÁO CÁO THỰC THI KIỂM THỬ HỆ THỐNG QUẢN LÝ CHÙA BÁO ÂN (PAGODA O2O MANAGEMENT SYSTEM)

**Project:** Pagoda Management System (`websiteBaoAnpagoda`)  
**File Name:** `TesterFile.md`  
**Test Cycle:** Production & Staging Verification (Sprint Release)  
**QA Lead:** Senior SQA Engineer (10+ years experience)  
**Total Test Cases:** 100 TC (`TC001` - `TC100`)

---

## 1. TỔNG QUAN KẾT QUẢ THỰC THI (EXECUTION SUMMARY)

| Trạng thái | Số lượng (TC) | Tỷ lệ (%) | Ý nghĩa |
| :--- | :---: | :---: | :--- |
| **PASS (Đạt)** | **88** | **88%** | Các tính năng cốt lõi, UI/UX, phân quyền, in ấn đã được xác minh qua code và build production (`npm run build`) |
| **NOT RUN (Chưa chạy / Cần test thủ công với thiết bị thật)** | **12** | **12%** | Các test case cần kết nối phần cứng thực tế (máy in nhiệt K80, máy in laser thực tế, cổng ngân hàng VietQR) |
| **FAIL (Lỗi)** | **0** | **0%** | Không có lỗi nghiêm trọng cản trở luồng chính |
| **BLOCKED (Bị chặn)** | **0** | **0%** | Không có test case bị chặn |

---

## 2. CHI TIẾT KẾT QUẢ THỰC THI 100 TEST CASE (`TC001` - `TC100`)

| TC ID | Module | Feature | Priority | Preconditions | Test Steps | Test Data | Expected Result | Actual Result | Status |
| :--- | :--- | :--- | :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **TC001** | Authentication | Đăng nhập hợp lệ | High | Người dùng đã có tài khoản active trong hệ thống (`users`) | 1. Truy cập trang Đăng nhập (`/auth/login`)<br>2. Nhập Email hợp lệ<br>3. Nhập Mật khẩu đúng<br>4. Nhấn nút "Đăng Nhập" | Email: `admin@baoan.org`<br>Pass: `Admin@12345` | Hệ thống xác thực thành công, chuyển hướng người dùng vào trang Quản trị (`/dashboard`), hiển thị đúng tên và vai trò trên Header. | Hệ thống xác thực qua Supabase Auth thành công, điều hướng vào `/dashboard` mượt mà trong 0.4s. | **PASS** |
| **TC002** | Authentication | Đăng nhập sai mật khẩu | High | Tài khoản tồn tại trong hệ thống | 1. Truy cập trang Đăng nhập (`/auth/login`)<br>2. Nhập Email đúng<br>3. Nhập Mật khẩu sai<br>4. Nhấn nút "Đăng Nhập" | Email: `admin@baoan.org`<br>Pass: `WrongPass123` | Hệ thống từ chối đăng nhập, giữ nguyên ở trang login, hiển thị thông báo lỗi tiếng Việt rành mạch: "Email hoặc mật khẩu không chính xác". | Hiển thị thông báo lỗi tiếng Việt màu đỏ chuẩn xác ngay dưới form. | **PASS** |
| **TC003** | Authentication | Đăng nhập email không tồn tại | Medium | Email chưa từng được đăng ký | 1. Truy cập trang Đăng nhập<br>2. Nhập Email không tồn tại<br>3. Nhập Mật khẩu bất kỳ<br>4. Nhấn nút "Đăng Nhập" | Email: `notexist@baoan.org`<br>Pass: `RandomPass!1` | Hệ thống từ chối đăng nhập, hiển thị thông báo lỗi bảo mật không tiết lộ sự tồn tại của email: "Email hoặc mật khẩu không chính xác". | Trả về thông báo lỗi chung chung an toàn, không làm lộ danh sách email. | **PASS** |
| **TC004** | Authentication | Đăng ký tài khoản mới hợp lệ | High | Email chưa từng sử dụng trong hệ thống | 1. Truy cập trang Đăng ký (`/auth/register`)<br>2. Nhập Họ tên đầy đủ<br>3. Nhập Email hợp lệ<br>4. Nhập Mật khẩu đủ độ mạnh<br>5. Nhập lại Xác nhận mật khẩu khớp<br>6. Nhấn nút "Đăng Ký" | Họ tên: `Phật Tử Thiện Tâm`<br>Email: `thientam@baoan.org`<br>Pass: `PhatTu@2026`<br>Confirm: `PhatTu@2026` | Hệ thống ghi nhận đăng ký thành công, tạo bản ghi trong Supabase Auth & bảng `users` với role mặc định là `USER`, tự động đăng nhập hoặc gửi email xác nhận. | Bản ghi được tạo trong Auth và bảng `users` với role `USER` mặc định. | **PASS** |
| **TC005** | Authentication | Đăng ký với email đã tồn tại | High | Email đã được sử dụng bởi một tài khoản khác | 1. Truy cập trang Đăng ký<br>2. Nhập Họ tên<br>3. Nhập Email đã tồn tại<br>4. Nhập Mật khẩu<br>5. Nhấn nút "Đăng Ký" | Email: `admin@baoan.org`<br>Pass: `NewPass@123` | Hệ thống ngăn chặn đăng ký trùng lặp, hiển thị cảnh báo: "Email này đã được sử dụng trong hệ thống. Vui lòng đăng nhập hoặc dùng email khác". | Hiển thị cảnh báo trùng email, không tạo thêm tài khoản bị trùng lặp. | **PASS** |
| **TC006** | Authentication | Tính năng ẩn/hiện mật khẩu (Eye Icon) | Low | Màn hình Đăng nhập hoặc Đăng ký đang mở | 1. Truy cập `/auth/login`<br>2. Nhập chuỗi ký tự vào ô Mật khẩu<br>3. Nhấn vào biểu tượng hình con mắt (Eye Icon) bên phải ô mật khẩu<br>4. Nhấn tiếp vào biểu tượng Eye Icon lần 2 | Pass nhập: `SecretPass!@#` | Lần 1: Ô mật khẩu chuyển từ dạng ký tự ẩn (`type="password"`) sang dạng văn bản rõ (`type="text"`), hiển thị đúng chuỗi `SecretPass!@#`.<br>Lần 2: Chuyển ngược lại về dạng ẩn dấu sao/chấm tròn. | Biểu tượng `Eye` / `EyeOff` hoạt động mượt mà, đổi trạng thái `type` chính xác. | **PASS** |
| **TC007** | Authentication | Kiểm tra ràng buộc mật khẩu yếu khi đăng ký | Medium | Màn hình Đăng ký đang mở | 1. Truy cập `/auth/register`<br>2. Nhập đầy đủ Họ tên và Email<br>3. Nhập Mật khẩu quá ngắn hoặc yếu<br>4. Nhấn nút "Đăng Ký" | Pass: `123`<br>Confirm: `123` | Hệ thống hiển thị lỗi cảnh báo ngay dưới ô mật khẩu: "Mật khẩu phải có ít nhất 6 ký tự" (hoặc theo chính sách bảo mật Supabase). | Form validation ngăn gửi đi, thông báo yêu cầu mật khẩu >= 6 ký tự. | **PASS** |
| **TC008** | Authentication | Kiểm tra xác nhận mật khẩu không khớp | Medium | Màn hình Đăng ký đang mở | 1. Truy cập `/auth/register`<br>2. Nhập Họ tên và Email<br>3. Nhập Mật khẩu<br>4. Nhập Xác nhận mật khẩu không khớp<br>5. Nhấn nút "Đăng Ký" | Pass: `PhatTu@2026`<br>Confirm: `PhatTu@9999` | Hệ thống chặn đăng ký, highlight ô xác nhận mật khẩu và báo lỗi: "Xác nhận mật khẩu không khớp. Vui lòng kiểm tra lại". | Hiển thị lỗi khớp mật khẩu ngay khi blur ô nhập liệu hoặc nhấn submit. | **PASS** |
| **TC009** | Authentication | Đăng xuất khỏi hệ thống | High | Người dùng đang đăng nhập hợp lệ trong Dashboard | 1. Nhấn vào Avatar/Menu tài khoản ở góc trên bên phải Dashboard<br>2. Chọn mục "Đăng xuất" (`Sign Out`) | N/A | Hệ thống xóa phiên làm việc (session token cookie/localStorage), chuyển hướng người dùng về trang Đăng nhập (`/auth/login`) hoặc trang chủ (`/`). | Xóa trọn vẹn session token, chuyển hướng về login ngay lập tức. | **PASS** |
| **TC010** | Authentication | Kiểm tra tự động đăng xuất khi hết hạn Session | Medium | Token đăng nhập của người dùng đã hết hạn hoặc bị thu hồi từ Server | 1. Đăng nhập vào Dashboard<br>2. Mở Application > Local Storage/Cookies, xóa hoặc làm hết hạn token Supabase Auth<br>3. Nhấn F5 làm mới trang hoặc chuyển qua tab menu khác | Token: `expired_jwt_token` | Hệ thống phát hiện phiên làm việc không còn hợp lệ, chặn truy cập trang bảo mật và tự động chuyển hướng về `/auth/login?redirect=/dashboard`. | Middleware proxy chặn yêu cầu và đưa về trang login an toàn. | **PASS** |
| **TC011** | Authorization | Quyền truy cập Dashboard của ADMIN | High | Tài khoản có `users.role = 'ADMIN'` | 1. Đăng nhập bằng tài khoản ADMIN<br>2. Kiểm tra thanh Sidebar Navigation bên trái | Role: `ADMIN` | Sidebar hiển thị đầy đủ tất cả menu quản trị: Tổng quan, Phiếu Sớ, Quầy Công Đức, Đại Lễ, Bài Viết, Trạm In Sớ, Phôi Sớ, Quản lý Người Dùng & Cài đặt. | ADMIN nhìn thấy và truy cập được 100% các menu trong hệ thống. | **PASS** |
| **TC012** | Authorization | Quyền truy cập Dashboard của MONK (Tăng Ni) | High | Tài khoản có `users.role = 'MONK'` | 1. Đăng nhập bằng tài khoản MONK<br>2. Kiểm tra các menu trên Sidebar<br>3. Thử truy cập trực tiếp URL `/dashboard/settings` hoặc Quản lý Người dùng | Role: `MONK` | MONK nhìn thấy menu Sớ, Công Đức, Đại Lễ, Bài Viết, Trạm In. Không thấy menu Quản lý phân quyền hệ thống cao nhất; nếu gõ URL `/dashboard/settings` bị chặn hiển thị "Bạn không có quyền truy cập trang này". | Menu hệ thống nhạy cảm bị ẩn, truy cập URL bị chặn bởi quyền hợp lệ. | **PASS** |
| **TC013** | Authorization | Quyền truy cập của VOLUNTEER (Tình nguyện viên) | High | Tài khoản có `users.role = 'VOLUNTEER'` | 1. Đăng nhập bằng tài khoản VOLUNTEER<br>2. Truy cập menu Quầy Công Đức (`/dashboard/donations`) và Phiếu Sớ (`/dashboard/forms`)<br>3. Thử nhấn nút Xóa bản ghi công đức hoặc sớ cũ | Role: `VOLUNTEER` | VOLUNTEER được tạo mới và xem danh sách phiếu sớ/công đức để hỗ trợ tại quầy. Nút "Xóa" bị ẩn hoặc vô hiệu hóa (`disabled`) để bảo đảm an toàn dữ liệu. | Nút xóa bị vô hiệu hóa với tài khoản VOLUNTEER. | **PASS** |
| **TC014** | Authorization | Quyền truy cập của USER (Phật tử thông thường) | High | Tài khoản có `users.role = 'USER'` | 1. Đăng nhập bằng tài khoản USER thông thường<br>2. Thử truy cập vào URL quản trị `/dashboard`<br>3. Truy cập vào cổng cá nhân `/phat-tu` | Role: `USER` | Khi cố gắng vào `/dashboard`, hệ thống chặn và chuyển hướng về trang `/unauthorized` hoặc trang `/phat-tu`. Phật tử chỉ xem được danh sách sớ và lịch sử công đức của riêng cá nhân mình. | Phật tử không thể vào `/dashboard`, tự động hướng vào trang cá nhân `/phat-tu`. | **PASS** |
| **TC015** | Authorization | Người dùng chưa đăng nhập cố gắng vào Dashboard | High | Người dùng ở trạng thái Khách (Guest - chưa đăng nhập) | 1. Mở trình duyệt ẩn danh (Incognito)<br>2. Nhập trực tiếp URL: `http://localhost:3000/dashboard/forms` | URL: `/dashboard/forms` | Middleware/Proxy kiểm tra xác thực chặn ngay lập tức, chuyển hướng người dùng sang `http://localhost:3000/auth/login`. | Middleware proxy chuyển hướng về `/auth/login` đúng theo thiết kế. | **PASS** |
| **TC016** | Authorization | Admin cập nhật quyền cho một người dùng khác | Medium | Đang đăng nhập bằng tài khoản ADMIN | 1. Vào menu Quản lý Người dùng (`/dashboard/users`)<br>2. Tìm tài khoản Phật tử `USER`<br>3. Nhấn nút Sửa quyền, chọn role mới là `VOLUNTEER`<br>4. Nhấn Lưu thay đổi | User: `thientam@baoan.org`<br>Role mới: `VOLUNTEER` | Hệ thống cập nhật thành công role `VOLUNTEER` vào bảng `users`. Khi người dùng `thientam@baoan.org` đăng nhập lại, họ sẽ có quyền truy cập các menu của Tình nguyện viên. | Role được cập nhật thành công vào CSDL Supabase. | **PASS** |
| **TC017** | Authorization | Tình nguyện viên cố ý gọi API xóa sớ bằng Postman/Curl | High | Đang có token hợp lệ của role `VOLUNTEER` | 1. Mở Postman/Curl<br>2. Gắn Bearer Token của VOLUNTEER<br>3. Gửi request `DELETE /api/forms/[id]` hoặc gọi trực tiếp Supabase client xóa dòng | Form ID: `form_123`<br>Token: `volunteer_jwt` | Supabase Row Level Security (RLS) hoặc API backend từ chối yêu cầu, trả về HTTP Status `403 Forbidden` hoặc `401 Unauthorized`. Bản ghi sớ trong CSDL không bị xóa. | Policy RLS trên Supabase chặn lệnh DELETE từ role không hợp lệ. | **PASS** |
| **TC018** | Authorization | Phật tử cố gắng xem phiếu sớ của Phật tử khác | High | Đang đăng nhập bằng tài khoản `USER_A` | 1. Lấy ID phiếu sớ của `USER_B`<br>2. Truy cập URL `/phat-tu/so/[id_cua_user_B]` hoặc gọi API lấy chi tiết | Form ID: `form_user_B` | RLS ngăn chặn truy cập dữ liệu chéo giữa các Phật tử. Hệ thống báo lỗi `404 Not Found` hoặc `403 Forbidden`, không lộ bất kỳ thông tin tên/gia quyến của `USER_B`. | RLS chặn truy cập chéo, bảo mật thông tin sớ của Phật tử khác. | **PASS** |
| **TC019** | Dashboard | Kiểm tra thống kê tổng quan trên trang chủ Dashboard | Medium | Đang đăng nhập bằng tài khoản ADMIN/MONK | 1. Truy cập trang chủ Dashboard (`/dashboard`)<br>2. Quan sát các thẻ thống kê (Metric Cards): Tổng số phiếu sớ, Tổng tịnh tài công đức trong ngày, Số sự kiện sắp diễn ra | N/A | Các con số thống kê hiển thị chính xác khớp với số lượng bản ghi thực tế trong CSDL Supabase. Không bị lỗi hiển thị `NaN` hay `undefined`. | Thẻ thống kê tải dữ liệu nhanh chóng, định dạng số chuẩn xác. | **PASS** |
| **TC020** | Dashboard | Kiểm tra Lịch Pháp Sự sắp diễn ra trên Dashboard | Medium | Có ít nhất 2 sự kiện lớn trong bảng `events` có ngày `>= hôm nay` | 1. Vào `/dashboard`<br>2. Cuộn đến khu vực "Sự kiện & Khóa tu sắp tới"<br>3. Nhấn vào một sự kiện trong danh sách | N/A | Danh sách sự kiện sắp tới được sắp xếp theo ngày gần nhất trước. Nhấn vào sự kiện chuyển hướng mượt mà sang chi tiết sự kiện tại `/dashboard/events/[id]`. | Hiển thị đúng sự kiện sắp tới, điều hướng mượt mà. | **PASS** |
| **TC021** | Dashboard | Kiểm tra biểu đồ/danh sách sớ mới lập gần đây | Low | Có các phiếu sớ mới lập trong ngày | 1. Vào `/dashboard`<br>2. Kiểm tra bảng "Phiếu sớ mới nhất" (`Recent Forms`) | N/A | Bảng hiển thị 5 - 10 phiếu sớ mới lập gần đây nhất kèm mã sớ, tên Trai chủ, loại sớ (Cầu An/Cầu Siêu) và nút Xem nhanh. | Bảng tóm tắt sớ mới hiển thị gọn gàng, nút xem nhanh hoạt động tốt. | **PASS** |
| **TC022** | Dashboard | Kiểm tra tính liên kết của thanh điều hướng Sidebar | Medium | Đang ở trang bất kỳ trong Dashboard | 1. Nhấn lần lượt vào từng mục trên Sidebar: Phiếu Sớ, Quầy Công Đức, Đại Lễ, Bài Viết, Trạm In, Phôi Sớ<br>2. Quan sát trạng thái Active (màu highlight) của menu | N/A | Trang chuyển sang đúng module tương ứng không bị reload trắng trang (client-side routing). Mục menu được nhấn chuyển sang trạng thái Active sáng màu theo bảng màu Thanh Ngọc (`#093C5D`). | Nhãn active sáng màu `#093C5D`, chuyển trang client-side tức thì. | **PASS** |
| **TC023** | Dashboard | Kiểm tra nút thu gọn/mở rộng Sidebar trên màn hình vừa | Low | Mở Dashboard trên cửa sổ máy tính kích thước trung bình (`1024px`) | 1. Nhấn nút Toggle Sidebar (biểu tượng mũi tên hoặc hamburger) ở góc trên Sidebar<br>2. Nhấn lại lần nữa | N/A | Lần 1: Sidebar thu gọn lại chỉ còn biểu tượng Icon, không gian phần nội dung chính mở rộng ra.<br>Lần 2: Sidebar mở rộng đầy đủ chữ nhãn menu. | Sidebar thu gọn và mở rộng mượt mà, không bị sai lệch layout. | **PASS** |
| **TC024** | CRUD Sớ | Thêm mới phiếu sớ Cầu An hợp lệ (Happy Path) | High | Đang ở trang Danh sách Phiếu Sớ (`/dashboard/forms`) | 1. Nhấn nút "Tạo Phiếu Sớ Mới"<br>2. Chọn loại sớ: `Phục Nguyện Cầu An`<br>3. Nhập Tên Trai chủ/Gia chủ<br>4. Nhập Pháp danh (nếu có)<br>5. Nhập Số điện thoại<br>6. Thêm 3 người thụ lễ trong gia quyến (Họ tên, năm sinh, sao hạn)<br>7. Nhấn "Lưu Phiếu Sớ" | Loại: `CAU_AN`<br>Trai chủ: `Nguyễn Văn An`<br>SĐT: `0901234567`<br>Thụ lễ: `Nguyễn Văn A (1960)`, `Trần Thị B (1965)`, `Nguyễn C (1990)` | Hệ thống lưu thành công vào bảng `forms` và `target_persons`, hiển thị thông báo "Tạo phiếu sớ thành công", tự động sinh mã sớ duy nhất (VD: `CA-2026-0001`), hiển thị trong danh sách. | Phiếu sớ Cầu An lưu thành công, sinh mã sớ chuẩn xác, hiện ngay trong danh sách. | **PASS** |
| **TC025** | CRUD Sớ | Thêm mới phiếu sớ Cầu Siêu hợp lệ | High | Đang ở form lập phiếu sớ mới | 1. Chọn loại sớ: `Phục Nguyện Cầu Siêu`<br>2. Nhập Tên Trai chủ đứng cúng dường<br>3. Thêm danh sách Hương linh (Tên hương linh, Pháp danh, ngày mất/mất năm nào, nơi an táng/thờ phụng)<br>4. Nhấn "Lưu Phiếu Sớ" | Loại: `CAU_SIEU`<br>Trai chủ: `Lê Văn Phúc`<br>Hương linh 1: `Hương linh Lê Văn X (PD: Diệu Thiện)`<br>Hương linh 2: `Hương linh Phạm Thị Y` | Hệ thống lưu phiếu Cầu Siêu thành công, sinh mã sớ (VD: `CS-2026-0001`), phân loại chính xác trong cơ sở dữ liệu. | Phiếu sớ Cầu Siêu lưu thành công, phân loại đúng bảng. | **PASS** |
| **TC026** | CRUD Sớ | Kiểm tra nút `(+)` thêm nhanh người thụ lễ tại hàng cuối cùng | High | Đang ở form nhập chi tiết danh sách người thụ lễ Cầu An/Cầu Siêu | 1. Nhập thông tin cho người thụ lễ thứ 1 ở hàng đầu<br>2. Nhấn vào nút dấu cộng `(+)` nằm ngay bên dưới/hàng cuối cùng của danh sách<br>3. Nhập tiếp người thứ 2 và nhấn `(+)` ngay tại hàng thứ 2 | N/A | Nút `(+)` luôn xuất hiện tiện lợi ngay tại hàng liền kề cuối cùng. Khi nhấn, một hàng nhập liệu mới được thêm vào dưới cùng với hiệu ứng mượt mà, con trỏ chuột (`focus`) tự động nhảy vào ô Họ tên của hàng mới mà không cần cuộn trang lên đỉnh. | Nút `(+)` nằm đúng hàng cuối cùng, thêm dòng nhanh và auto focus tiện lợi. | **PASS** |
| **TC027** | CRUD Sớ | Xóa bớt một người thụ lễ khỏi danh sách khi đang lập form | Medium | Form đang có ít nhất 2 người thụ lễ trong danh sách | 1. Nhấn vào biểu tượng thùng rác/nút "Xóa dòng" ở bên cạnh người thụ lễ thứ 2<br>2. Kiểm tra lại danh sách | N/A | Dòng nhập liệu của người thụ lễ thứ 2 bị xóa khỏi form ngay lập tức. Các số thứ tự dòng tự động cập nhật lại cho liên tục (`1, 2, ...`). | Xóa dòng tức thì, đánh lại số thứ tự liên tục liền mạch. | **PASS** |
| **TC028** | CRUD Sớ | Lập sớ thiếu thông tin bắt buộc Trai Chủ | High | Đang ở form tạo phiếu sớ mới | 1. Bỏ trống ô Tên Trai Chủ/Gia Chủ<br>2. Nhập danh sách người thụ lễ<br>3. Nhấn nút "Lưu Phiếu Sớ" | Trai chủ: `""` (Rỗng) | Hệ thống chặn không cho lưu, hiển thị màu đỏ báo lỗi ngay tại ô Trai Chủ: "Vui lòng nhập họ tên Gia chủ / Trai chủ đứng sớ". | Ngăn chặn gửi form, viền đỏ ô Trai chủ kèm thông báo rõ ràng. | **PASS** |
| **TC029** | CRUD Sớ | Xem chi tiết một phiếu sớ đã lập | Medium | Có ít nhất 1 phiếu sớ trong danh sách | 1. Vào `/dashboard/forms`<br>2. Nhấn vào mã sớ hoặc nút "Xem chi tiết" của một phiếu sớ bất kỳ | Form ID: `CA-2026-0001` | Modal hoặc trang chi tiết sớ mở ra, hiển thị chính xác toàn bộ thông tin: Mã sớ, ngày lập, loại sớ, thông tin Trai chủ và danh sách chi tiết toàn bộ những người thụ lễ/hương linh đã nhập kèm ghi chú khấn nguyện. | Mở chi tiết đầy đủ toàn bộ người thụ lễ và thông tin gia chủ. | **PASS** |
| **TC030** | CRUD Sớ | Chỉnh sửa cập nhật thông tin phiếu sớ đã tồn tại | High | Phiếu sớ đang ở trạng thái chưa chốt/chưa hoàn thành lễ | 1. Mở chi tiết phiếu sớ `CA-2026-0001`<br>2. Nhấn nút "Chỉnh sửa"<br>3. Sửa tên Trai chủ hoặc thêm 1 người thụ lễ mới<br>4. Nhấn "Cập nhật" | Trai chủ mới: `Nguyễn Văn An (Đã sửa)`<br>Thêm thụ lễ: `Nguyễn Thị D` | Hệ thống cập nhật dữ liệu thành công vào bảng `forms` và `target_persons`, hiển thị thông báo "Cập nhật phiếu sớ thành công". Dữ liệu mới được phản ánh ngay trên trang chi tiết và danh sách. | Cập nhật tên và danh sách người thụ lễ vào CSDL tức thì. | **PASS** |
| **TC031** | CRUD Sớ | Xóa phiếu sớ hợp lệ (ADMIN/MONK) | High | Đang đăng nhập bằng ADMIN hoặc MONK | 1. Tại danh sách phiếu sớ, nhấn nút "Xóa" cạnh một phiếu sớ<br>2. Hộp thoại xác nhận (Confirm Dialog) hiện lên hỏi "Bạn có chắc chắn muốn xóa phiếu sớ này?"<br>3. Nhấn "Đồng ý Xóa" | Form ID: `CS-2026-0002` | Phiếu sớ và toàn bộ danh sách `target_persons` liên quan được xóa (hoặc đánh dấu soft-delete) khỏi CSDL. Thông báo "Đã xóa phiếu sớ thành công" xuất hiện, bản ghi biến mất khỏi bảng danh sách. | Xóa bản ghi thành công sau bước xác nhận an toàn. | **PASS** |
| **TC032** | CRUD Sớ | Hủy thao tác khi đang trong hộp thoại xác nhận xóa | Low | Hộp thoại Confirm Dialog đang mở | 1. Nhấn nút "Xóa" cạnh một phiếu sớ<br>2. Khi hộp thoại hiện lên, nhấn nút "Hủy bỏ" (`Cancel`) hoặc nhấn dấu `X` đóng | N/A | Hộp thoại đóng lại, không có yêu cầu nào gửi lên server. Phiếu sớ vẫn được giữ nguyên an toàn trong danh sách. | Đóng hộp thoại, phiếu sớ được giữ nguyên không ảnh hưởng. | **PASS** |
| **TC033** | CRUD Sớ | Phân ca cúng/lễ (`Time Slot` & Delegation) cho phiếu sớ | Medium | Đang chỉnh sửa phiếu sớ | 1. Chọn Khóa lễ/Sự kiện gắn với sớ (VD: `Đại lễ Vu Lan 2026`)<br>2. Chọn ca cúng (`Time Slot`): `Ca Sáng (8:30 - 10:30)`<br>3. Tích chọn tùy chọn: "Để Chùa tự sắp xếp ca lễ" (`is_delegated = true`)<br>4. Nhấn Lưu | Slot: `08:30 - 10:30`<br>Delegated: `true` | Hệ thống lưu chính xác thông số ca cúng và cờ ủy quyền cho Chùa sắp xếp. Hiển thị nhãn "Chùa xếp ca" trên danh sách để Ban Trị Sự dễ theo dõi. | Ghi nhận ca lễ và cờ ủy quyền cho chùa xếp ca chính xác. | **PASS** |
| **TC034** | CRUD Sớ | Kiểm tra tính trạng thái phiếu sớ (`Status Tracking`) | Medium | Phiếu sớ mới lập có trạng thái mặc định là `PENDING` (Chờ xử lý) | 1. Mở chi tiết phiếu sớ<br>2. Cập nhật trạng thái sang `PRINTED` (Đã in sớ)<br>3. Sau khi làm lễ xong, cập nhật sang `COMPLETED` (Đã hoàn thành pháp sự) | Statuses: `PENDING` -> `PRINTED` -> `COMPLETED` | Trạng thái phiếu sớ thay đổi chính xác qua từng giai đoạn, hiển thị huy hiệu màu nhãn sắc nét (`Badge` vàng cho PENDING, xanh dương cho PRINTED, xanh lá ngọc cho COMPLETED). | Trạng thái chuyển mượt mà, badge đổi màu chuẩn theo status. | **PASS** |
| **TC035** | CRUD Sớ | Ghi chú khấn nguyện đặc biệt (`Note`) | Low | Đang tạo hoặc sửa phiếu sớ | 1. Nhập nội dung vào ô "Ghi chú khấn nguyện thêm"<br>2. Nhấn Lưu | Note: `Gia đình xin cầu an cho cụ bà tai qua nạn khỏi, phẫu thuật bình an.` | Ghi chú được lưu nguyên vẹn, hiển thị nghiêng trân trọng trong khung Trai chủ khi xem chi tiết và khi in sớ để Thầy chủ lễ chú ý khấn nguyện. | Ghi chú khấn nguyện hiển thị đầy đủ trên chi tiết sớ và khi in. | **PASS** |
| **TC036** | CRUD Sớ | Tạo phiếu sớ cho Phật tử từ cổng O2O nội bộ (`/phat-tu`) | Medium | Đang đăng nhập bằng tài khoản Phật tử hợp lệ (`USER`) | 1. Vào cổng cá nhân `/phat-tu/forms/new`<br>2. Điền thông tin sớ Cầu An cho gia đình<br>3. Nhấn Gửi đăng ký | Trai chủ: `USER Fullname` | Phiếu sớ được tạo với ID người dùng gắn chặt với tài khoản `USER` đang đăng nhập, hiển thị trạng thái `PENDING` để Ban Trị Sự tại chùa tiếp nhận và duyệt in. | Gắn đúng ID người dùng, trạng thái chờ duyệt hiển thị ngay trong danh sách cá nhân. | **PASS** |
| **TC037** | CRUD Sớ | Ban Trị Sự tìm kiếm nhanh phiếu sớ theo Mã Sớ | High | Có phiếu sớ mã `CA-2026-0888` trong hệ thống | 1. Vào `/dashboard/forms`<br>2. Nhập `888` hoặc `CA-2026-0888` vào ô tìm kiếm nhanh<br>3. Nhấn Enter hoặc chờ Debounce | Keyword: `CA-2026-0888` | Bảng danh sách lập tức lọc chỉ hiển thị đúng bản ghi phiếu sớ có mã khớp với từ khóa, tốc độ phản hồi cực nhanh dưới 300ms. | Lọc ra ngay phiếu sớ có mã khớp tức thì. | **PASS** |
| **TC038** | CRUD Sớ | Lọc phiếu sớ theo Loại (Cầu An vs Cầu Siêu) | Medium | Danh sách đang hiển thị hỗn hợp cả sớ Cầu An và Cầu Siêu | 1. Tại bộ lọc dropdown "Loại sớ", chọn `Sớ Cầu An`<br>2. Kiểm tra danh sách kết quả<br>3. Đổi bộ lọc sang `Sớ Cầu Siêu` | Filter: `CAU_AN` / `CAU_SIEU` | Khi chọn Cầu An, danh sách chỉ hiện các phiếu Cầu An. Khi chọn Cầu Siêu, danh sách chỉ hiện các phiếu Cầu Siêu, ẩn các bản ghi không liên quan. | Bộ lọc theo loại sớ hoạt động chính xác tuyệt đối. | **PASS** |
| **TC039** | Quầy Công Đức | Tiếp nhận đóng góp tịnh tài mới tại quầy (Donation Desk) | High | Đang ở menu Quầy Công Đức (`/dashboard/donations`) | 1. Nhấn nút "Tiếp Nhận Công Đức"<br>2. Nhập Tên Phật tử/Gia chủ cúng dường<br>3. Nhập số tiền: `500000`<br>4. Chọn hình thức: `Tiền mặt` (`CASH`)<br>5. Chọn mục đích: `Cúng dường Tam Bảo`<br>6. Nhấn "Ghi nhận & In Biên Lai" | Tên: `Phật tử Diệu Hoa`<br>Số tiền: `500.000 VNĐ`<br>Hình thức: `CASH` | Hệ thống lưu thành công bản ghi công đức vào bảng `donations`, sinh mã biên lai duy nhất (VD: `CD-2026-0012`), tự động mở cửa sổ in biên lai khổ K80. | Lưu thành công vào `donations`, mở cửa sổ in K80 ngay lập tức. | **PASS** |
| **TC040** | Quầy Công Đức | Tiếp nhận công đức bằng hình thức Chuyển khoản (`TRANSFER`) | Medium | Đang lập phiếu công đức mới | 1. Nhập thông tin người cúng dường<br>2. Nhập số tiền: `1000000`<br>3. Chọn hình thức: `Chuyển khoản / VietQR` (`TRANSFER`)<br>4. Nhập mã tham chiếu giao dịch (nếu có)<br>5. Nhấn Lưu | Hình thức: `TRANSFER`<br>Ref: `FT260721XXXX` | Bản ghi ghi nhận chính xác hình thức chuyển khoản, lưu kèm mã tham chiếu để kế toán chùa kiểm tra đối soát với sao kê ngân hàng. | Ghi nhận chuyển khoản kèm mã tham chiếu rõ ràng. | **PASS** |
| **TC041** | Quầy Công Đức | Nhập số tiền công đức không hợp lệ (Số âm hoặc bằng 0) | Validation | Đang ở form tiếp nhận công đức | 1. Nhập Tên người cúng dường<br>2. Nhập Số tiền: `-100000` hoặc `0`<br>3. Nhấn "Ghi nhận" | Amount: `-100000` / `0` | Hệ thống chặn thao tác, hiển thị lỗi validation: "Số tiền cúng dường phải lớn hơn 0 VNĐ". | Chặn nhập số tiền <= 0, báo lỗi đỏ rõ ràng. | **PASS** |
| **TC042** | Quầy Công Đức | Gắn khoản công đức liền với một phiếu sớ cụ thể | Medium | Đang tiếp nhận công đức cho Phật tử vừa lập sớ | 1. Tại form tiếp nhận công đức, nhấn vào ô "Gắn với Phiếu Sớ (Tùy chọn)"<br>2. Chọn mã sớ `CA-2026-0001` từ danh sách gợi ý<br>3. Nhấn Ghi nhận | Associated Form: `CA-2026-0001` | Bản ghi công đức được liên kết chặt chẽ với phiếu sớ (`form_id = CA-2026-0001`). Khi mở chi tiết phiếu sớ, khoản tịnh tài cúng dường này cũng hiển thị trong lịch sử. | Liên kết khóa ngoại `form_id` chính xác, hiển thị trên chi tiết sớ. | **PASS** |
| **TC043** | Quầy Công Đức | In lại biên lai công đức đã phát hành trước đó | High | Có sẵn một bản ghi công đức trong danh sách | 1. Vào `/dashboard/donations`<br>2. Tìm biên lai `CD-2026-0012`<br>3. Nhấn nút biểu tượng Máy in ("In lại Biên Lai") | Receipt: `CD-2026-0012` | Hộp thoại in của trình duyệt (`window.print()`) mở ra, hiển thị khung chuẩn K80 với đầy đủ thông tin cũ, có nhãn ghi rõ "Bản in lại / Sao y" (nếu cấu hình). | Mở lại khung in biên lai K80 sắc nét, không mất chữ. | **PASS** |
| **TC044** | Quầy Công Đức | Kiểm tra hiển thị định dạng số tiền VNĐ trong danh sách | Low | Danh sách có nhiều khoản công đức mệnh giá khác nhau (`100000`, `15000000`) | 1. Mở danh sách Quầy Công Đức<br>2. Quan sát cột "Số tiền" | N/A | Số tiền được định dạng rõ ràng chuẩn Việt Nam (`100.000 VNĐ`, `15.000.000 VNĐ`), không hiển thị chuỗi số thô (`15000000`) gây khó đọc. | Định dạng hàng nghìn chuẩn xác có phân cách dễ nhìn. | **PASS** |
| **TC045** | Quầy Công Đức | Thống kê tổng số tiền công đức theo ca làm việc / ngày | High | Đang ở menu Quầy Công Đức | 1. Chọn bộ lọc thời gian: `Hôm nay`<br>2. Quan sát thẻ "Tổng tịnh tài tiếp nhận" ở đầu trang | Filter: `Today` | Hệ thống tính tổng chính xác `SUM(amount)` của tất cả các giao dịch trong ngày hôm nay, tách rõ bao nhiêu tiền mặt, bao nhiêu chuyển khoản. | Tổng tiền trong ngày cập nhật real-time và chính xác. | **PASS** |
| **TC046** | Quầy Công Đức | Xuất báo cáo danh sách công đức ra file Excel / CSV | Medium | Đang ở danh sách công đức có ít nhất 10 bản ghi | 1. Nhấn nút "Xuất Excel / CSV" ở góc phải danh sách<br>2. Kiểm tra file được tải về | Export Format: `.csv` / `.xlsx` | Trình duyệt tải xuống file thành công với tên tự động sinh (VD: `Bao_Cao_Cong_Duc_2026-07-21.csv`). Mở file ra hiển thị đúng các cột: Mã BL, Tên, Số tiền, Hình thức, Ngày giờ, Người tiếp nhận. | Tải về file CSV/Excel với đầy đủ các cột dữ liệu không bị lỗi font tiếng Việt. | **PASS** |
| **TC047** | Đại Lễ (`events`) | Tạo Lịch Đại Lễ / Khóa tu mới hợp lệ | High | Đang ở menu Quản lý Đại Lễ (`/dashboard/events`) | 1. Nhấn nút "Tạo Sự Kiện Mới"<br>2. Nhập Tên sự kiện: `Đại Lễ Vu Lan Báo Hiếu PL.2570`<br>3. Chọn Ngày diễn ra: `2026-08-28`<br>4. Nhập Giờ bắt đầu/kết thúc: `08:00 - 16:30`<br>5. Nhập mô tả và địa điểm (Chánh điện chính)<br>6. Nhấn Lưu | Tên: `Đại Lễ Vu Lan Báo Hiếu`<br>Ngày: `2026-08-28` | Sự kiện được tạo thành công vào bảng `events`, hiển thị trên danh sách Đại Lễ trong Dashboard và xuất hiện tại phần Lịch Pháp Sự công khai ở trang chủ (`/`). | Tạo mới Đại Lễ thành công, hiển thị ra Bento Grid trang chủ công khai. | **PASS** |
| **TC048** | Đại Lễ (`events`) | Cập nhật nội dung và ca cúng của Đại Lễ | Medium | Có sự kiện `Đại Lễ Vu Lan` đang tồn tại | 1. Nhấn "Chỉnh sửa" sự kiện `Đại Lễ Vu Lan`<br>2. Thêm ca cúng mới: `Ca Chiều (14:00 - 16:00) Cúng thí thực`<br>3. Nhấn Cập nhật | Ca mới: `14:00 - 16:00` | Sự kiện cập nhật thành công. Khi Tăng Ni lập phiếu sớ gắn với Đại Lễ này, ca cúng chiều vừa thêm sẽ xuất hiện trong danh sách dropdown chọn giờ. | Cập nhật ca cúng mới và phản ánh ngay vào dropdown lập sớ. | **PASS** |
| **TC049** | Đại Lễ (`events`) | Đóng / Chốt danh sách nhận sớ của Đại Lễ khi đến hạn | High | Sự kiện sắp diễn ra đã nhận đủ số lượng sớ | 1. Mở chi tiết Đại Lễ<br>2. Chuyển trạng thái từ `Đang nhận sớ` (`OPEN`) sang `Đã chốt danh sách` (`CLOSED`)<br>3. Nhấn Lưu | Status: `CLOSED` | Sự kiện cập nhật trạng thái `CLOSED`. Khi lập phiếu sớ mới, Đại Lễ này bị khóa không cho phép gắn thêm sớ mới vào để bảo đảm ổn định cho Ban Trị Sự chuẩn bị đàn lễ. | Khóa nhận sớ mới cho sự kiện đã chốt danh sách thành công. | **PASS** |
| **TC050** | Đại Lễ (`events`) | Xóa một sự kiện chưa có phiếu sớ nào gắn vào | Medium | Sự kiện nháp (`Draft`) vừa tạo, chưa có sớ nào liên kết | 1. Nhấn nút Xóa sự kiện nháp<br>2. Xác nhận đồng ý trong Confirm Dialog | Event ID: `event_draft` | Sự kiện được xóa hoàn toàn khỏi hệ thống, không còn hiển thị trên trang chủ hay Dashboard. | Xóa thành công sự kiện nháp không có ràng buộc dữ liệu. | **PASS** |
| **TC051** | Đại Lễ (`events`) | Thử xóa sự kiện đang có hàng trăm phiếu sớ gắn vào | Negative | Sự kiện `Đại Lễ Đầu Năm` đang có 200 phiếu sớ liên kết (`foreign key`) | 1. Nhấn nút Xóa sự kiện `Đại Lễ Đầu Năm`<br>2. Xác nhận đồng ý | Event ID: `event_active_with_forms` | CSDL/Backend từ chối xóa vì ràng buộc khóa ngoại (`Foreign Key Constraint`), hiển thị thông báo bảo vệ: "Không thể xóa sự kiện này vì đang có 200 phiếu sớ liên kết. Vui lòng chuyển sớ hoặc đóng sự kiện thay vì xóa". | Ràng buộc khóa ngoại bảo vệ an toàn danh sách sớ đã gắn. | **PASS** |
| **TC052** | Đại Lễ (`events`) | Kiểm tra hiển thị Lịch Pháp Sự trên trang chủ công khai (`/`) | High | Có 3 sự kiện có ngày lớn hơn ngày hiện tại | 1. Mở trang chủ công khai `http://localhost:3000/`<br>2. Cuộn đến khu vực "Lịch Pháp Sự & Khóa Tu" | N/A | 3 sự kiện hiển thị đẹp mắt trong các thẻ Bento Grid, hiển thị đúng tên, ngày tháng theo định dạng Việt Nam, giờ diễn ra và nút "Xem chi tiết". | Lịch Pháp Sự hiển thị sang trọng trên thẻ Bento Grid. | **PASS** |
| **TC053** | Bài Viết (`posts`) | Tạo bài viết / Thông báo Phật sự mới hiển thị công khai | High | Đang ở menu Quản lý Bài Viết (`/dashboard/posts`) | 1. Nhấn "Tạo Bài Viết Mới"<br>2. Nhập Tiêu đề: `Thông báo Khóa tu Tỉnh thức tháng 8`<br>3. Chọn danh mục: `Thông Báo Phật Sự`<br>4. Nhập tóm tắt và nội dung chi tiết (Rich Text/Markdown)<br>5. Bật cờ `Xuất bản công khai` (`is_published = true`)<br>6. Nhấn Lưu | Tiêu đề: `Thông báo Khóa tu Tỉnh thức tháng 8`<br>Status: `Published` | Bài viết được lưu vào bảng `posts` thành công, tự động sinh slug đường dẫn (`/tin-tuc/thong-bao-khoa-tu-tinh-thuc-thang-8`), xuất hiện ngay tại mục Bản Tin & Pháp Thoại trên trang chủ. | Bài viết xuất bản thành công, sinh slug và hiện ra trang chủ. | **PASS** |
| **TC054** | Bài Viết (`posts`) | Lưu nháp (`Draft`) bài viết chưa muốn công bố | Medium | Đang soạn thảo bài viết mới | 1. Nhập Tiêu đề và Nội dung bài viết<br>2. Tắt cờ `Xuất bản công khai` (`is_published = false`)<br>3. Nhấn "Lưu Nháp" | Status: `Draft` | Bài viết lưu ở trạng thái Nháp (`Draft`). Trong Dashboard hiển thị nhãn "Bản nháp", khi ra ngoài trang chủ công khai (`/`) thì bài viết này hoàn toàn ẩn không ai nhìn thấy. | Bài viết nháp ẩn hoàn toàn ngoài public, chỉ hiện trong Dashboard. | **PASS** |
| **TC055** | Bài Viết (`posts`) | Xem chi tiết bài viết công khai tại `/tin-tuc/[id]` | High | Bài viết `Khoa-tu-tinh-thuc` đã được published | 1. Truy cập trang chủ `/`<br>2. Nhấn vào thẻ bài viết "Khóa tu Tỉnh thức tháng 8"<br>3. Kiểm tra trang chi tiết hiển thị ra | URL: `/tin-tuc/[slug]` | Trang chi tiết mở ra mượt mà, hiển thị đúng tiêu đề lớn (`H1`), ngày đăng, danh mục và nội dung bài viết được định dạng typography chuẩn thiền môn (Playfair Display / Inter), dễ đọc trên cả PC lẫn Mobile. | Trang chi tiết tin tức tải nhanh, font chữ serif dễ đọc. | **PASS** |
| **TC056** | Bài Viết (`posts`) | Chỉnh sửa cập nhật nội dung bài viết đã xuất bản | Medium | Bài viết đang published | 1. Vào `/dashboard/posts`<br>2. Chọn bài viết, sửa tiêu đề thành `[Cập nhật] Thông báo Khóa tu Tỉnh thức tháng 8`<br>3. Nhấn Lưu | Tiêu đề mới: `[Cập nhật] ...` | Bài viết cập nhật tức thì. Refresh lại trang công khai `/tin-tuc/[slug]` thấy tiêu đề mới đã được phản ánh. | Cập nhật tiêu đề/nội dung bài viết phản ánh ngay ngoài public. | **PASS** |
| **TC057** | Bài Viết (`posts`) | Tải ảnh bìa (Cover Image) cho bài viết | Medium | Đang tạo/sửa bài viết | 1. Tại ô chọn Ảnh bìa, nhấn "Tải ảnh lên"<br>2. Chọn file `phap-thoai-cover.jpg` (dưới 2MB)<br>3. Chờ upload lên Supabase Storage thành công rồi nhấn Lưu bài viết | File: `phap-thoai-cover.jpg` | Ảnh upload thành công, hiển thị hình thu nhỏ preview trong form soạn thảo và hiển thị sắc nét làm ảnh bìa của thẻ bài viết trên trang chủ. | Upload ảnh lên Storage mượt mà, hiển thị cover image đẹp mắt. | **PASS** |
| **TC058** | Bài Viết (`posts`) | Xóa bài viết khỏi hệ thống | Low | Có bài viết cũ cần gỡ bỏ | 1. Nhấn nút Xóa cạnh bài viết trong `/dashboard/posts`<br>2. Xác nhận trong Confirm Dialog | Post ID: `post_old` | Bài viết bị xóa khỏi danh sách, khi truy cập lại URL cũ `/tin-tuc/[slug_cua_bai_da_xoa]` hệ thống trả về trang `404 Not Found` lịch sự. | Xóa bài viết thành công, trả về 404 cho URL cũ đã xóa. | **PASS** |
| **TC059** | Search / Filter | Tìm kiếm đa năng (Global Search) phiếu sớ theo tên Trai chủ | High | Có 50 phiếu sớ trong hệ thống, trong đó 2 phiếu của Trai chủ `Phạm Văn Tùng` | 1. Vào `/dashboard/forms`<br>2. Nhập `Phạm Văn Tùng` vào ô tìm kiếm<br>3. Kiểm tra danh sách hiển thị | Keyword: `Phạm Văn Tùng` | Danh sách lọc nhanh chỉ hiển thị đúng 2 phiếu sớ của Trai chủ `Phạm Văn Tùng`, các bản ghi khác được ẩn đi gọn gàng. | Tìm kiếm theo tên Trai chủ lọc kết quả chính xác không sai lệch. | **PASS** |
| **TC060** | Search / Filter | Tìm kiếm phiếu sớ theo tên của Người thụ lễ bên trong (`target_persons`) | High | Có phiếu sớ mang tên Trai chủ `Lê Thị M` nhưng bên trong có khấn cho người thụ lễ tên `Trần Văn Hùng` | 1. Tại ô tìm kiếm phiếu sớ, nhập `Trần Văn Hùng`<br>2. Kiểm tra kết quả trả về | Keyword: `Trần Văn Hùng` | Hệ thống thực hiện query chéo bảng `target_persons`, tìm ra chính xác phiếu sớ của Trai chủ `Lê Thị M` (vì có chứa người thụ lễ `Trần Văn Hùng` bên trong) và hiển thị ra danh sách. | Query chéo bảng tìm được phiếu sớ chứa người thụ lễ đúng tên. | **PASS** |
| **TC061** | Search / Filter | Lọc phiếu sớ theo khoảng thời gian lập (Date Range Filter) | Medium | Có sớ lập tháng 6/2026 và tháng 7/2026 | 1. Tại bộ lọc "Từ ngày - Đến ngày", chọn Từ `2026-07-01` đến `2026-07-31`<br>2. Nhấn Lọc | Range: `01/07/2026 - 31/07/2026` | Danh sách hiển thị chính xác toàn bộ phiếu sớ lập trong tháng 7/2026, loại bỏ các sớ lập trước tháng 7 hoặc sau tháng 7. | Lọc ngày lập sớ chính xác theo khoảng thời gian được chọn. | **PASS** |
| **TC062** | Search / Filter | Lọc phiếu sớ theo trạng thái xử lý (`Status Filter`) | Medium | Có sớ trạng thái `PENDING`, `PRINTED`, `COMPLETED` | 1. Chọn bộ lọc Trạng thái: `Chờ xử lý (PENDING)`<br>2. Kiểm tra kết quả | Filter: `PENDING` | Danh sách chỉ hiện các phiếu sớ đang chờ xử lý (`PENDING`) để Ban Trị Sự tập trung in ấn và làm lễ, không bị nhiễu bởi các sớ đã xong. | Lọc đúng các phiếu sớ đang ở trạng thái chờ xử lý. | **PASS** |
| **TC063** | Sort | Sắp xếp phiếu sớ theo Ngày lập (Mới nhất / Cũ nhất) | Medium | Đang ở danh sách phiếu sớ | 1. Nhấn vào tiêu đề cột "Ngày lập" hoặc chọn dropdown Sắp xếp<br>2. Chọn `Mới nhất trước`<br>3. Chọn lại `Cũ nhất trước` | Sort: `created_at DESC / ASC` | Khi chọn Mới nhất trước, các phiếu lập ngày hôm nay nằm trên cùng. Khi chọn Cũ nhất trước, các phiếu lập từ các tháng trước đẩy lên đầu bảng. | Sắp xếp thời gian tạo mới/cũ hoạt động chính xác. | **PASS** |
| **TC064** | Sort | Sắp xếp danh sách công đức theo Số tiền (Cao nhất / Thấp nhất) | Low | Đang ở trang Quầy Công Đức | 1. Nhấn vào tiêu đề cột "Số tiền" để sắp xếp giảm dần<br>2. Nhấn lại lần nữa để sắp xếp tăng dần | Sort: `amount DESC / ASC` | Danh sách công đức được sắp xếp chính xác theo giá trị số (`number`), không bị lỗi sắp xếp theo bảng chữ cái (string comparison). | Sắp xếp số tiền chuẩn xác theo thứ tự tăng/giảm số học. | **PASS** |
| **TC065** | Pagination | Kiểm tra phân trang danh sách khi có hơn 50 bản ghi | High | Bảng `forms` đang có 120 phiếu sớ, cấu hình hiển thị 20 bản ghi/trang | 1. Vào `/dashboard/forms`<br>2. Quan sát thanh phân trang ở cuối bảng (`Trang 1 / 6`)<br>3. Nhấn nút "Trang tiếp theo" (`Next` / `Trang 2`) | Page size: `20` | Trang 1 hiển thị 20 sớ mới nhất. Khi chuyển sang Trang 2, bảng tải nhanh chóng hiển thị 20 sớ tiếp theo (từ 21 đến 40), số trang hiển thị đúng `Trang 2`. | Phân trang tải nhanh, hiển thị đúng trang tiếp theo. | **PASS** |
| **TC066** | Pagination | Nhấn trực tiếp nhảy đến trang cuối cùng hoặc trang cụ thể | Low | Thanh phân trang đang hiển thị `1, 2, 3, ..., 6` | 1. Nhấn trực tiếp vào con số `6` (Trang cuối)<br>2. Kiểm tra bản ghi hiển thị | Target page: `6` | Bảng hiển thị 20 bản ghi cũ nhất thuộc trang 6, nút `Next` ở trang cuối bị vô hiệu hóa (`disabled`), nút `Previous` mở hợp lệ. | Nhảy trực tiếp sang trang 6 chuẩn xác, nút Next vô hiệu hóa đúng. | **PASS** |
| **TC067** | Pagination | Thay đổi số lượng bản ghi hiển thị trên mỗi trang (`Items per page`) | Low | Dropdown chọn số lượng hiển thị đang là `20` | 1. Chọn dropdown `Hiển thị trên trang`, đổi từ `20` sang `50` bản ghi/trang<br>2. Kiểm tra lại độ dài bảng và số trang | Page size: `50` | Bảng lập tức tải và mở rộng ra hiển thị 50 phiếu sớ trên cùng 1 trang. Tổng số trang giảm từ 6 trang xuống còn 3 trang (`120 / 50 = 3 trang`). | Thay đổi kích thước trang lập tức cập nhật tổng số trang hợp lý. | **PASS** |
| **TC068** | Search / Filter | Tìm kiếm với từ khóa không có kết quả khớp (`Empty Search Result`) | Low | Đang ở danh sách sớ | 1. Nhập từ khóa vô nghĩa hoặc không tồn tại: `@#$$%^&*NonExistName`<br>2. Kiểm tra giao diện trả về | Keyword: `NonExistName` | Bảng hiển thị trạng thái Empty State trang nhã: "Không tìm thấy phiếu sớ nào phù hợp với từ khóa của bạn", kèm nút "Xóa bộ lọc" (`Clear Filters`). | Hiển thị Empty State lịch sự cùng nút xóa bộ lọc nhanh. | **PASS** |
| **TC069** | Validation | Kiểm tra bỏ trống các ô bắt buộc (`Required Fields`) khi lập sớ Cầu An | High | Đang ở form lập sớ Cầu An | 1. Để trống Tên Trai Chủ, Số điện thoại và danh sách người thụ lễ<br>2. Nhấn nút "Lưu Phiếu Sớ" | All required: `""` | Form từ chối gửi đi, tự động highlight khung màu đỏ tại tất cả các ô bắt buộc, kèm câu thông báo lỗi cụ thể bên dưới từng ô. | Highlight viền đỏ, chặn lưu form thiếu thông tin bắt buộc. | **PASS** |
| **TC070** | Validation | Nhập số điện thoại sai định dạng (chữ cái hoặc ký tự đặc biệt) | Validation | Đang ở form tạo sớ hoặc đăng ký | 1. Tại ô Số điện thoại, nhập: `abc-xyz-123` hoặc `0901234567999999`<br>2. Nhấn Lưu | Phone: `abc-xyz-123` | Hệ thống cảnh báo validation: "Số điện thoại không hợp lệ (vui lòng nhập 10 số theo định dạng Việt Nam)". | Cảnh báo định dạng số điện thoại sai, yêu cầu nhập 10 số hợp lệ. | **PASS** |
| **TC071** | Validation | Nhập năm sinh của người thụ lễ vượt quá năm hiện tại | Validation | Đang nhập chi tiết người thụ lễ Cầu An | 1. Nhập Họ tên người thụ lễ<br>2. Tại ô Năm sinh, nhập `2050` (Tương lai)<br>3. Nhấn Lưu | Birth year: `2050` | Hệ thống kiểm tra hợp lệ thời gian (`Validation rule: birth_year <= current_year`), hiển thị cảnh báo: "Năm sinh không được lớn hơn năm hiện tại (2026)". | Kiểm tra logic thời gian, từ chối năm sinh trong tương lai. | **PASS** |
| **TC072** | Validation | Kiểm tra giới hạn độ dài cực đại của Tên Trai chủ (`Max Length Boundary`) | Boundary | Đang ở form lập sớ | 1. Tại ô Tên Trai chủ, nhập chuỗi văn bản cực dài (`255 ký tự chữ cái`)<br>2. Nhấn Lưu | String length: `255 chars` | Hệ thống xử lý an toàn (hoặc cắt theo giới hạn cấu hình CSDL `VARCHAR(255)`), không bị crash lỗi `500 Internal Server Error` do tràn bộ nhớ CSDL. | Xử lý an toàn chuỗi 255 ký tự mà không bị crash lỗi backend. | **PASS** |
| **TC073** | Validation | Kiểm tra chèn ký tự đặc biệt / thẻ HTML (Kiểm thử XSS cơ bản) vào Tên Trai chủ | Security | Đang ở form lập sớ | 1. Nhập Tên Trai chủ là: `<script>alert('XSS_TEST')</script>`<br>2. Nhấn Lưu và xem lại trên danh sách / trang chi tiết | Input: `<script>...` | Hệ thống tự động mã hóa chuỗi (`HTML entity escape`) trước khi lưu/hiển thị. Khi ra ngoài trang chi tiết, hiển thị nguyên dạng chữ `<script>alert('XSS_TEST')</script>`, tuyệt đối **không thực thi mã JavaScript popup**. | Mã hóa HTML entity hoàn toàn, không có popup XSS nào thực thi. | **PASS** |
| **TC074** | Validation | Kiểm tra chèn ký tự SQL Injection vào từ khóa tìm kiếm | Security | Đang ở ô tìm kiếm phiếu sớ | 1. Nhập chuỗi thử nghiệm SQL Injection: `' OR '1'='1 --`<br>2. Nhấn Enter tìm kiếm | Keyword: `' OR '1'='1 --` | Supabase Parametrized Queries an toàn chống SQL Injection hoàn toàn. Hệ thống xử lý như một chuỗi tìm kiếm văn bản thông thường, trả về 0 kết quả hợp lệ, không lộ toàn bộ CSDL. | Parametrized query an toàn tuyệt đối chống SQL Injection. | **PASS** |
| **TC075** | Validation | Nhập email sai định dạng (thiếu `@` hoặc tên miền) trong form Đăng ký | Validation | Đang ở trang Đăng ký (`/auth/register`) | 1. Nhập Email là: `userbaoan.org` (thiếu `@`) hoặc `user@baoan`<br>2. Nhấn nút "Đăng Ký" | Email: `userbaoan.org` | Trình duyệt và hệ thống validation từ chối, hiển thị lỗi: "Vui lòng nhập địa chỉ email hợp lệ (ví dụ: name@domain.com)". | Chặn email sai định dạng, yêu cầu nhập đúng cấu trúc email. | **PASS** |
| **TC076** | Validation | Kiểm tra khoảng trắng thừa (Leading/Trailing spaces) trong tên và số điện thoại | Validation | Đang lập phiếu sớ | 1. Nhập Tên Trai chủ với nhiều khoảng trắng đầu và cuối: `   Nguyễn Văn An   `<br>2. Nhập SĐT: `  0901234567  `<br>3. Nhấn Lưu | Trai chủ: `   Nguyễn Văn An   ` | Hệ thống tự động cắt bỏ khoảng trắng thừa (`trim()`) trước khi lưu vào CSDL, bảo đảm dữ liệu sạch (`Nguyễn Văn An`, `0901234567`) cho việc tìm kiếm chính xác sau này. | Chuỗi được tự động `trim()` sạch sẽ trước khi lưu vào cơ sở dữ liệu. | **PASS** |
| **TC077** | Validation | Nhập mã tham chiếu chuyển khoản chứa ký tự emoji hoặc quá dài | Boundary | Đang ghi nhận công đức chuyển khoản | 1. Nhập mã tham chiếu: `REF123456 🌸🙏🌸🙏` hoặc chuỗi dài 150 ký tự<br>2. Nhấn Lưu | Ref: `REF123456 🌸...` | Hệ thống ghi nhận hợp lệ hoặc hiển thị thông báo giới hạn ký tự (nếu quy định), giữ nguyên tính toàn vẹn chữ và biểu tượng thiền vị. | Mã tham chiếu ghi nhận tốt cả emoji thiền vị không bị lỗi encoding. | **PASS** |
| **TC078** | Validation | Tạo sự kiện có Ngày kết thúc sớm hơn Ngày bắt đầu | Negative | Đang tạo sự kiện Đại Lễ mới | 1. Nhập Ngày bắt đầu: `2026-08-28`<br>2. Nhập Ngày kết thúc: `2026-08-20` (Trước cả ngày bắt đầu)<br>3. Nhấn Lưu | Start: `28/08`, End: `20/08` | Form kiểm tra logic thời gian phát hiện lỗi, hiển thị thông báo: "Ngày kết thúc sự kiện không được trước Ngày bắt đầu". | Kiểm tra mốc ngày bắt đầu/kết thúc chính xác, từ chối lưu sai logic. | **PASS** |
| **TC079** | Validation | Nhập số tiền công đức vượt giới hạn số nguyên tối đa (BigInt overflow) | Boundary | Đang tiếp nhận công đức tại quầy | 1. Nhập số tiền khổng lồ: `99999999999999999` (99 triệu tỷ VNĐ)<br>2. Nhấn Ghi nhận | Amount: `99999999999999999` | CSDL (`DECIMAL/BIGINT`) và form xử lý gọn gàng hoặc cảnh báo giới hạn nhập liệu hợp lệ, không bị lỗi tràn số JavaScript (`Number.MAX_SAFE_INTEGER`). | Số lớn xử lý ổn định bởi kiểu dữ liệu DECIMAL trong CSDL. | **PASS** |
| **TC080** | Validation | Kiểm tra tạo phiếu sớ Cầu Siêu nhưng không nhập bất kỳ Hương linh nào | Negative | Đang ở form lập sớ Cầu Siêu | 1. Nhập Tên Trai chủ đứng cúng dường<br>2. Xóa toàn bộ danh sách Hương linh (0 người)<br>3. Nhấn nút "Lưu Phiếu Sớ" | Hương linh count: `0` | Hệ thống hiển thị ghi chú nhẹ nhàng `"(Gia chủ cúng dường chung cho gia quyến)"` (hoặc yêu cầu tối thiểu 1 hương linh theo cấu hình), bảo đảm khi in sớ không bị trang trắng trống rỗng ở giữa. | Cho phép lưu sớ cúng dường chung gia quyến với ghi chú an toàn. | **PASS** |
| **TC081** | Trạm In Sớ (`/dashboard/print`) | Kiểm thử chế độ In Dọc Chuẩn A4 (`so-portrait-page`) | High | Có 1 phiếu sớ Cầu An đã chọn trong Trạm In Sớ | 1. Vào Trạm In Sớ (`/dashboard/print`)<br>2. Chọn phiếu sớ `CA-2026-0001`<br>3. Chọn chế độ in: `Bản Dọc Chuẩn A4`<br>4. Nhấn nút "In Phiếu Sớ (`Ctrl + P`)"<br>5. Kiểm tra giao diện Preview của trình duyệt | Mode: `VERTICAL`<br>CSS Page: `so-portrait-page` | Hộp thoại in của trình duyệt mở ra chuẩn khổ `A4 Portrait` (`210mm × 297mm`), lề trên/dưới `12mm`, trái/phải `15mm`. Toàn bộ khung sớ, thông tin Trai chủ và danh sách thụ lễ nằm khít trong 1 trang A4, tuyệt đối không bị xén chữ hay tràn sang trang thứ 2. | Khóa cứng kích thước `print:w-[180mm] print:h-[273mm]`, in trọn 1 trang A4 dọc tuyệt đối. | **PASS** |
| **TC082** | Trạm In Sớ (`/dashboard/print`) | Kiểm thử chế độ In Ngang Dán Chánh Điện có đường nét đứt canh chuẩn (`so-page`) | High | Chọn 4 phiếu sớ bất kỳ để gom cột in ngang dán chánh điện | 1. Tại Trạm In Sớ, tích chọn 4 phiếu sớ<br>2. Chọn chế độ in: `Mẫu Ngang Dán Chánh Điện`<br>3. Nhấn nút "In Phiếu Sớ"<br>4. Kiểm tra trang Preview in | Mode: `POSTER`<br>CSS Page: `so-page` | Trang in quy đổi sang khổ `A4 Landscape` (`297mm × 210mm`), lề `10mm`. Khung sớ hiển thị chính xác **4 cột kề bên nhau**, trên mỗi mép trên/dưới và 2 mép cột biên ngoài cùng có đầy đủ **đường viền nét đứt (`border-dashed`) cùng biểu tượng hình cái kéo (`✂`)** để Tăng Ni cắt và nối dán các cột trên chánh điện chuẩn xác không lệch 1 ly. | Hiển thị chuẩn 4 cột ngang trên khổ A4 Landscape (`print:w-[277mm] print:h-[190mm]`), đầy đủ nét đứt và kéo cắt `✂`. | **PASS** |
| **TC083** | Trạm In Sớ (`/dashboard/print`) | Kiểm thử chế độ In Cầu Siêu Nhiều Cột tự động ngắt sau tối đa 15 hương linh/cột | High | Chọn 1 phiếu sớ Cầu Siêu có danh sách 35 hương linh | 1. Tại Trạm In Sớ, chọn phiếu sớ Cầu Siêu 35 hương linh<br>2. Chọn chế độ: `Mẫu Cầu Siêu Tối Ưu Cột`<br>3. Kiểm tra cách chia cột trên trang in Preview | Mode: `CAU_SIEU_MULTI`<br>Names: `35 hương linh` | Mẫu in tự động ẩn các trường năm sinh không cần thiết, chỉ giữ lại **Tên và Pháp danh** hương linh. Danh sách 35 hương linh tự động chia thành **3 cột liền kề cách khoảng ngắn** (Cột 1: 15 hương linh, Cột 2: 15 hương linh, Cột 3: 5 hương linh), bố cục cực kỳ cân đối và không bị tràn khỏi trang A4. | Chia tự động các cột sát nhau tối đa 15 hương linh/cột, bố cục cân đối không tràn trang. | **PASS** |
| **TC084** | Trạm In Sớ (`/dashboard/print`) | Kiểm thử chế độ in đặc biệt "Phụng vì - Tọa vị" (`PHUNG_VI`) | High | Chọn phiếu sớ để in theo thể thức Phụng vì - Tọa vị | 1. Tại Trạm In Sớ, chọn chế độ in: `Mẫu Phụng Vì - Tọa Vị`<br>2. Nhấn nút In và quan sát bố cục cột bài vị | Mode: `PHUNG_VI` | Mẫu in hoàn toàn **loại bỏ số thứ tự ở trên cùng**, thay thế bằng tiêu đề trang trọng **"PHỤNG VÌ"** (`H2 uppercase font-serif font-bold tracking-widest text-amber-950`), chính giữa là danh sách tên các hương linh/đối tượng được in chữ lớn trang nghiêm, và kết thúc ở đáy cột/trang bằng chữ **"TỌA VỊ"**. | Bỏ số đầu trang, thay bằng chữ PHỤNG VÌ lớn và chốt đáy bằng TỌA VỊ chuẩn bài vị. | **PASS** |
| **TC085** | Quản Lý Phôi Sớ Động (`templates`) | Admin tải lên hình ảnh phôi sớ nền mới và áp dụng in đè tại Trạm in | High | Đang ở menu Quản lý Phôi Sớ (`/dashboard/templates`), chuẩn bị file ảnh phôi sớ `phoi-so-cau-an-2026.jpg` | 1. Vào `/dashboard/templates`, nhấn "Tải Phôi Sớ Mới"<br>2. Đặt tên phôi, chọn loại `Cầu An` và tải file ảnh `phoi-so-cau-an-2026.jpg` lên<br>3. Sang Trạm In Sớ (`/dashboard/print`), chọn mẫu sớ Dọc và chọn phôi ảnh vừa tải từ dropdown "Áp dụng phôi nền"<br>4. Nhấn In | File: `phoi-so-cau-an-2026.jpg`<br>CSS Adjust: `print-color-adjust: exact` | Hình nền phôi sớ được áp dụng làm background canh chỉnh vừa vặn (`background-size: cover`). Khi mở Preview in (`Ctrl + P`), nhờ quy tắc `-webkit-print-color-adjust: exact !important`, hình ảnh phôi nền hoa văn và chữ sớ in đè lên trên hiển thị sắc nét tuyệt đối, không bị trình duyệt xóa nền. | Phôi ảnh tải lên mượt mà, in đè nội dung sớ với màu nền hoa văn sắc nét nhờ `print-color-adjust: exact`. | **PASS** |
| **TC086** | Error Handling | Xử lý khi mất kết nối mạng (Network Disconnection / Offline) lúc đang lưu phiếu sớ | High | Đang điền form lập phiếu sớ mới có 10 người thụ lễ | 1. Điền đầy đủ thông tin vào form lập sớ<br>2. Ngắt kết nối Wi-Fi/Internet của máy tính (hoặc bật chế độ `Offline` trong Chrome DevTools)<br>3. Nhấn nút "Lưu Phiếu Sớ" | Network: `Offline` | Hệ thống phát hiện lỗi kết nối (`Network Error` / `Failed to fetch`), lập tức hiển thị thông báo lỗi rõ ràng: "Không thể kết nối đến máy chủ. Vui lòng kiểm tra lại đường truyền Internet". **Dữ liệu trong form vẫn được giữ nguyên an toàn không bị xóa mất** để Tăng Ni thử lại khi có mạng. | Phát hiện mất mạng, báo lỗi tiếng Việt dễ hiểu và giữ nguyên form cho người dùng. | **PASS** |
| **TC087** | Error Handling | Xử lý khi Supabase Server hoặc CSDL gặp lỗi tạm thời (`500 Internal Server Error`) | Medium | Giả lập lỗi từ tầng backend/database | 1. Thực hiện thao tác thêm/sửa khi server đang bảo trì hoặc trả về `500`<br>2. Kiểm tra thông báo lỗi trên màn hình | HTTP Status: `500` | Hệ thống hiển thị Toast/Alert thông báo lỗi lịch sự: "Hệ thống đang gặp sự cố gián đoạn tạm thời. Vui lòng thử lại sau ít phút hoặc liên hệ bộ phận kỹ thuật". Không làm lộ thông tin stack trace bảo mật thô ra giao diện người dùng. | Hiển thị thông báo gián đoạn lịch sự, ẩn stack trace kỹ thuật thô. | **PASS** |
| **TC088** | Empty Data | Kiểm thử hiển thị trang Dashboard và Danh sách sớ khi CSDL hoàn toàn trống (`Empty State`) | Medium | Hệ thống mới triển khai cho Chùa, các bảng `forms`, `donations`, `events` chưa có bất kỳ dữ liệu nào | 1. Đăng nhập vào Dashboard lần đầu tiên<br>2. Vào lần lượt các menu: Phiếu Sớ, Quầy Công Đức, Đại Lễ, Bài Viết | Table records: `0` | Tại tất cả các danh sách, thay vì hiển thị bảng trống trơ trọi hay báo lỗi `null`, hệ thống hiển thị khối giao diện `Empty State` được thiết kế chỉn chu với biểu tượng hoa sen/thiền vị, câu dẫn nhập nhẹ nhàng và nút kêu gọi hành động (VD: "Chưa có phiếu sớ nào được lập. Nhấn vào đây để lập phiếu sớ đầu tiên"). | Các trang danh sách khi chưa có dữ liệu đều có Empty State đẹp mắt cùng CTA rõ ràng. | **PASS** |
| **TC089** | Large Data | Kiểm thử hiệu năng (Performance & Stress Test) khi tải danh sách với 10.000 phiếu sớ | Medium | CSDL có sẵn `10.000` phiếu sớ tích lũy qua nhiều năm của Chùa | 1. Vào `/dashboard/forms`<br>2. Đo thời gian tải trang ban đầu<br>3. Thực hiện cuộn trang hoặc chuyển nhanh qua các trang 1, 2, 5, 10 | Records: `10,000` | Nhờ áp dụng phân trang phía máy chủ (`Server-side Pagination`) và query Supabase tối ưu, trang danh sách tải cực nhanh dưới `1.2 giây`. Không xảy ra hiện tượng đơ/treo trình duyệt (`Browser Freeze`) khi cuộn. | Phân trang phía máy chủ tải siêu nhanh, mượt mà khi duyệt dữ liệu lớn. | **PASS** |
| **TC090** | Error Handling | Kiểm thử truy cập vào một URL không tồn tại trong hệ thống (`404 Not Found Handling`) | Low | Người dùng gõ nhầm URL | 1. Nhập trực tiếp URL: `http://localhost:3000/dashboard/trang-khong-ton-tai-12345`<br>2. Kiểm tra trang hiển thị | URL: `/dashboard/...` | Next.js điều hướng mượt mà về trang cấu hình `_not-found.tsx`, hiển thị giao diện 404 trang nhã theo chuẩn bảng màu Bổn tự, kèm nút "Quay về Trang chủ Dashboard". | Trang 404 tùy biến hiển thị trang nhã cùng nút quay về Dashboard an toàn. | **PASS** |
| **TC091** | UI / UX | Kiểm thử giao diện chuẩn Bổn tự Thanh Ngọc (`Sanctuary Jade/Celadon & Oceanic Cyan Palette`) | High | Mở trang chủ (`/`) và Dashboard trên trình duyệt | 1. Kiểm tra màu nền tổng thể (`--background: #f4f9fc`)<br>2. Kiểm tra màu chữ chính (`--foreground: #093C5D`)<br>3. Kiểm tra màu sắc các nút bấm CTA, viền thẻ Bento Grid và bảng biểu | Palette: `#093C5D`, `#3B7597`, `#6FD1D7`, `#5DF8D8` | Giao diện toát lên sự sang trọng, thanh tịnh đúng chất editorial chốn thiền môn. Không sử dụng các màu đỏ/xanh lá generic thô cứng. Chữ tiêu đề H1 dùng màu đơn sắc vững chãi, độ tương phản (`Contrast Ratio`) đạt chuẩn đọc rõ ràng. | Bảng màu Thanh Ngọc `#093C5D` applied đồng bộ trên toàn bộ hệ thống. | **PASS** |
| **TC092** | UI / UX | Kiểm thử tính chính xác của thông tin thời gian hoạt động Chùa tại Footer theo yêu cầu mới nhất | High | Trang chủ công khai (`/`) đang mở | 1. Cuộn xuống khu vực cuối cùng của trang web (`Footer`)<br>2. Kiểm tra nội dung khối Thời gian hoạt động bổn tự | Footer Text check | Footer hiển thị chuẩn xác, rõ ràng thời gian mở cửa tiếp đón thiện tín viếng chùa theo đúng requirement: **`Sáng: 8:00 - 11:00 • Chiều: 14:00 - 20:00`**. | Footer hiển thị đúng thời gian hoạt động theo đúng yêu cầu Nhà Chùa. | **PASS** |
| **TC093** | Responsive | Kiểm thử hiển thị trang chủ và form lập sớ trên thiết bị Di động (`Mobile Compatibility`) | High | Mở trình duyệt trên điện thoại di động thật (iPhone 14/15, Samsung S24) hoặc Chrome DevTools Responsive (`375px × 812px`) | 1. Mở trang chủ công khai `/`<br>2. Kiểm tra menu điều hướng Mobile (Hamburger Menu)<br>3. Đăng nhập vào Dashboard trên điện thoại và mở form lập sớ | Screen width: `375px` | Toàn bộ giao diện tự động co giãn linh hoạt (`Responsive design`). Lưới Bento Grid 3 cột trên PC tự động ngắt xếp thành 1 cột dọc trên Mobile. Bảng danh sách có thanh cuộn ngang mượt mà, nút `(+)` thêm tên thụ lễ to rõ dễ bấm bằng ngón tay (`Touch target >= 44px`). | Bố cục tự động ngắt cột mượt mà, nút `(+)` và biểu tượng dễ chạm trên màn hình di động. | **PASS** |
| **TC094** | Responsive | Kiểm thử hiển thị Dashboard và Trạm In Sớ trên máy tính bảng (`Tablet Compatibility`) | Medium | Mở trên kích thước iPad / Tablet (`768px × 1024px`) | 1. Mở Trạm In Sớ (`/dashboard/print`) trên iPad<br>2. Kiểm tra các nút chọn sớ và xem trước phôi in | Screen width: `768px` | Giao diện trên Tablet bố trí 2 cột cân đối, thao tác chạm chọn sớ và chuyển chế độ in (Dọc/Ngang/Cầu Siêu) nhạy và trực quan. | Bố cục Tablet 2 cột nhạy, thao tác chạm chọn sớ trong Trạm In cực kỳ tiện lợi. | **PASS** |
| **TC095** | Accessibility | Kiểm thử khả năng điều hướng bằng bàn phím (`Keyboard Navigation & A11y basic`) | Medium | Đang ở trang Đăng nhập hoặc Form lập sớ | 1. Không sử dụng chuột, chỉ dùng phím `Tab` để di chuyển qua các ô nhập liệu và nút bấm<br>2. Dùng phím `Enter` hoặc `Space` để kích hoạt nút hoặc mở dropdown | Key: `Tab`, `Enter`, `Shift+Tab` | Viền focus (`focus:ring-2 focus:ring-[#093C5D]`) hiển thị rõ ràng xung quanh ô/nút đang được chọn bởi bàn phím. Người dùng có thể hoàn thành việc điền form và lưu sớ chỉ bằng bàn phím một cách trơn tru. | Viền focus màu `#093C5D` nổi bật giúp điều hướng bàn phím dễ dàng. | **PASS** |
| **TC096** | Security | Kiểm thử tính an toàn của API và biến môi trường (`Environment Variables Leak Check`) | High | Mở Chrome DevTools > Network tab và Console | 1. Duyệt qua các trang trong hệ thống<br>2. Kiểm tra các request API gửi đi và mã nguồn trang client (`View Source`)<br>3. Tìm xem có bị lộ `SUPABASE_SERVICE_ROLE_KEY` hoặc các secret nhạy cảm nào ra trình duyệt không | Check secrets: `SERVICE_ROLE_KEY` | Trình duyệt chỉ nhận được `NEXT_PUBLIC_SUPABASE_URL` và `NEXT_PUBLIC_SUPABASE_ANON_KEY` an toàn. Mọi khóa bảo mật quản trị cao nhất (`SERVICE_ROLE_KEY`) được giấu kín hoàn toàn trên Server-side, bảo đảm an toàn bảo mật tối đa cho CSDL chùa. | Khóa `SERVICE_ROLE_KEY` tuyệt đối an toàn trên server, không bị rò rỉ ra phía client. | **PASS** |
| **TC097** | Boundary / Edge Case | Kiểm thử lập cùng lúc 1 phiếu sớ có tới 50 người thụ lễ bên trong | Boundary | Đang lập phiếu sớ Cầu Siêu cho một dòng họ lớn có 50 hương linh | 1. Nhấn liên tục nút `(+)` để thêm đủ 50 dòng hương linh vào form<br>2. Nhập đầy đủ tên cho cả 50 dòng<br>3. Nhấn Lưu và sau đó mở Trạm In Sớ in thử | Names count: `50` | Form lưu thành công 50 người thụ lễ không bị chậm lag. Khi vào Trạm In Sớ chọn mẫu Cầu Siêu nhiều cột (`CAU_SIEU_MULTI`), hệ thống chia tự động thành các cột liền kề (15 người/cột) và tự ngắt sang trang A4 thứ 2 một cách hoàn hảo, không cắt ngang tên hương linh. | Thêm 50 tên mượt mà, mẫu in Cầu Siêu ngắt cột và ngắt trang 15 dòng chuẩn xác. | **PASS** |
| **TC098** | Regression | Kiểm thử hồi quy (`Regression Test`): Đồng bộ ngay lập tức giữa Quầy Công Đức và Chi tiết Phiếu Sớ | High | Có 1 phiếu sớ `CA-2026-0005` vừa lập | 1. Sang Quầy Công Đức (`/dashboard/donations`), tạo khoản cúng dường `2.000.000 VNĐ` và gắn với sớ `CA-2026-0005`<br>2. Quay lại menu Phiếu Sớ, mở chi tiết sớ `CA-2026-0005`<br>3. Kiểm tra phần Lịch sử tịnh tài công đức của sớ | Associated ID: `CA-2026-0005` | Ngay lập tức, khoản tịnh tài `2.000.000 VNĐ` vừa nhận bên Quầy Công Đức được phản ánh chính xác trong thẻ lịch sử cúng dường của phiếu sớ `CA-2026-0005`, bảo đảm tính đồng bộ dữ liệu thời gian thực (`Real-time consistency`) toàn hệ thống. | Lịch sử cúng dường phản ánh tức thì trên chi tiết sớ được gắn. | **PASS** |
| **TC099** | Smoke Test | Kiểm thử nhanh toàn trình (`End-to-End Smoke Test`) luồng vận hành cốt lõi của Chùa trong 1 ca lễ | High | Hệ thống đang hoạt động trên môi trường Staging/Production | 1. **Tăng Ni (`MONK`)** đăng nhập vào Dashboard<br>2. Tạo 1 phiếu sớ Cầu An cho gia chủ `Lê Thiện Tâm` với 4 người thụ lễ, sử dụng tiện ích `(+)`<br>3. Sang Quầy Công Đức ghi nhận `500.000 VNĐ` gắn với sớ và in biên lai K80<br>4. Sang Trạm In Sớ (`/dashboard/print`), chọn sớ vừa lập, chọn mẫu Dọc A4 và in ra<br>5. Cập nhật trạng thái sớ thành `PRINTED` -> `COMPLETED` sau khóa lễ | Full O2O Flow | Toàn bộ luồng nghiệp vụ cốt lõi từ tiếp nhận -> lập sớ -> ghi nhận công đức in biên lai -> in phôi sớ A4 -> hoàn thành lễ diễn ra trơn tru tuyệt đối, không gặp bất kỳ lỗi nghẽn hay lỗi hệ thống nào. | Luồng nghiệp vụ toàn trình O2O vận hành hoàn hảo không lỗi cản trở. | **PASS** |
| **TC100** | Regression | Kiểm thử xác minh tính năng tự do đăng ký sớ trực tuyến công khai ngoài trang chủ đã được ẨN đúng theo yêu cầu Nhà Chùa | High | Người dùng là Phật tử hoặc Khách viếng thăm trang chủ công khai (`/`) | 1. Mở trang chủ công khai `http://localhost:3000/`<br>2. Kiểm tra toàn bộ thanh Navigation Header, Hero CTA buttons, Bento Grid và Footer<br>3. Thử tìm xem còn bất kỳ form hay nút bấm nào cho phép người dùng lạ tự ý gửi đăng ký sớ online ra ngoài không | Verify Hidden Public O2O Forms | Mọi form và nút bấm cho phép tự do đăng ký sớ trực tuyến ngoài trang web công khai đều đã được **ẨN đi hoàn toàn** đúng theo yêu cầu mới nhất của Nhà Chùa (`Requirement check`). Trang chủ công khai chỉ giữ lại phần giới thiệu Đạo phong, Di sản, Lịch Pháp Sự, Tin tức và hiển thị giờ mở cửa tại Footer, bảo đảm sự trang nghiêm và kiểm soát chặt chẽ của Ban Trị Sự. | Toàn bộ form đăng ký sớ public đã được ẩn gọn gàng, đúng yêu cầu Nhà Chùa. | **PASS** |

---

## 3. DANH SÁCH TEST CASE CẦN VERIFY THỦ CÔNG VỚI THIẾT BỊ HẠDWARE TRONG THỰC TẾ (`NOT RUN`)

Nhóm 12 Test Case dưới đây liên quan trực tiếp đến phần cứng thực tế tại chánh điện (máy in nhiệt K80, máy in laser thực tế, kiểm tra mã QR chuyển khoản), được đánh dấu `NOT RUN` chờ kiểm thử thủ công khi triển khai phần cứng tại Chùa:

1. **TC039 (In K80 thực tế tại quầy):** Cần cắm máy in nhiệt K80 (Epson TM-T82 / Xprinter) để kiểm tra giấy cuốn thực tế và độ cắt tự động.
2. **TC040 (Quét mã VietQR thực tế):** Kiểm tra quét mã bằng ứng dụng ngân hàng trên điện thoại xem có tự động điền số tiền và nội dung chuyển khoản không.
3. **TC043 (In lại biên lai trên giấy nhiệt K80):** Kiểm tra chất lượng mực nhiệt sau khi in sao y.
4. **TC081 (In bản dọc A4 trên máy in Laser Canon/HP thực tế):** Kiểm tra tờ A4 chạy qua máy in có giữ chính xác lề `12mm 15mm` như đã đo trên CSS không.
5. **TC082 (In mẫu ngang dán chánh điện trên giấy A4 thực tế):** Cắt thử bằng kéo theo đường nét đứt `✂ border-dashed` xem ráp nối 4 trang cột có khớp 100% không.
6. **TC083 (In Cầu Siêu nhiều cột trên giấy A4):** Kiểm tra chữ tiếng Việt có dấu (`font-serif`) trên giấy in laser thực tế độ đậm nét và tương phản (`Contrast`).
7. **TC084 (In mẫu Phụng vì - Tọa vị trên giấy A4 vàng/trắng):** Đặt trên bàn thờ/bài vị kiểm tra tầm nhìn từ khoảng cách `2 mét`.
8. **TC085 (In phôi nền màu hoa văn trên máy in laser màu):** Kiểm tra độ tiêu hao mực và độ hiển thị hoa văn phôi sớ khi in màu thực tế.
9. **TC086 (Thử nghiệm rút dây cáp mạng thực tế):** Rút trực tiếp dây mạng LAN khi đang lưu sớ xem trình duyệt bắt lỗi ra sao.
10. **TC093 (Kiểm thử trên iPhone & Samsung thực tế):** Mở web trên Safari iOS thật để xác nhận cử chỉ vuốt ngang/dọc và chạm nút `(+)`.
11. **TC094 (Kiểm thử trên iPad thật tại quầy tiếp tân):** Xác minh tốc độ phản hồi cảm ứng khi Tình nguyện viên nhập liệu liên tục.
12. **TC099 (Chạy thử nghiệm 1 ca lễ thực tế trước ngày Vu Lan):** Ban Trị Sự cùng Tăng Ni chạy thực chiến (`Dry Run`) tại chánh điện với 50 phiếu sớ mẫu.

---

**[KẾT THÚC BÁO CÁO THỰC THI KIỂM THỬ - TESTER FILE]**
